---
description: "业务知识 - 项目背景、领域术语、业务规则；当需要理解业务上下文时使用"
alwaysApply: false
---
# 业务知识

## 项目背景

<!-- TODO: 补充项目起因、目标用户、你在项目中负责的模块，以及是否有前端仓库 / 在线演示地址。 -->

## 领域术语

| 术语 | 代码名 | 业务含义 |
| --- | --- | --- |
| 接口信息 | `InterfaceInfo` | 平台可开放给调用方使用的 API 元数据 |
| 调用关系 | `UserInterfaceInfo` | 用户对某个接口的剩余次数、总调用次数、状态 |
| 访问密钥 | `accessKey` | 调用方身份标识 |
| 签名密钥 | `secretKey` | 生成签名的私密凭证，不应在请求中明文传输 |

## 核心业务规则

<!-- TODO: 补充接口计费 / 免费额度 / 管理员审核 / 调用失败是否扣次数等规则。 -->

```mermaid
flowchart LR
    User[用户注册] --> Key[分配 AK/SK]
    Key --> Invoke[调用接口]
    Invoke --> Count[扣减剩余次数]
```

